# Core - required for filesystem to work
fusepy>=3.0.1
pywin32>=306; sys_platform == 'win32'
sqlalchemy>=2.0.0
lz4>=4.3.0
networkx>=3.0
numpy>=1.24.0
rich>=13.0.0
pyyaml>=6.0
psutil>=5.9.0

# Optional - AI/ML (adds ~1GB, enables semantic search)
# sentence-transformers>=2.2.0
