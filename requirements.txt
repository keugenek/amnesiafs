# Core - required for filesystem to work
fusepy>=3.0.1
pywin32>=306; sys_platform == 'win32'
sqlalchemy>=2.0.0
lz4>=4.3.0
networkx>=3.0
numpy>=1.24.0
rich>=13.0.0
pyyaml>=6.0
psutil>=5.9.0

# AI/ML - enables semantic search with embeddings
# Default model: BAAI/bge-base-en-v1.5 (768 dims, ~440MB)
# Set COGNITIVEFS_EMBEDDING_MODEL env var to change model
sentence-transformers>=2.2.0
